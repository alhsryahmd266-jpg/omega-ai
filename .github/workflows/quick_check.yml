name: Quick Check
on:
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - run: pip install requests -q
      - env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python3 scripts/check_again.py
